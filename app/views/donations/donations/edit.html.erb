<section id="main" class="container">
  <div class="box">
    <h3>Send a Donation</h3>
    <%= render partial: 'shared/donation_errors', locals: { donation: @donation } if @donation.errors.any? %>
    <%= render 'form', donation: @donation %>
  </div>
</section>
